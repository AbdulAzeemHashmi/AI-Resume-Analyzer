{
  "buildCommand": "cd frontend && npm install && npm run build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/api.py"
    },
    {
      "source": "/(.*)",
      "destination": "/frontend/$1"
    }
  ]
}